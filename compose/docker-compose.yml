version: '2'

services:
    app-db:
        image: mysql
        networks:
            - backend

    app-db-schema:
        image: dockerbrownbag-schema
        links:
            - app-db
        depends_on:
            - app-db
        networks:
            - backend

    rest-services:
        image: rest-services
        ports: 
            - 80:80
        links:
            - app-db
        depends_on:
            - app-db
        networks:
            - frontend
            - backend
networks:
    backend:
        driver: bridge
    frontend:
        driver: bridge


